node_modules
dist
data

# CI / dev artifacts
coverage
.nyc_output
test-results
playwright-report
playwright/.cache
blob-report
*.tsbuildinfo

# Tests are not needed in the runtime image (Dockerfile only copies src/
# + server/ explicitly, but keeping these out of the build context shaves
# upload time too).
tests

# VCS / editor / OS noise
.git
.gitignore
.github
.vscode
.idea
.DS_Store
Thumbs.db

# Internal tooling not needed for the image
.claude
.husky
scripts
design
reviews

# Env files — Fly reads secrets via `fly secrets`, not committed files
.env
.env.local
.env.*.local

# Docs (image only needs runtime code)
*.md
